# Dashboard Guide

Справочник по всем кнопкам и функциям Projects Health Dashboard.

## Общая информация

**Путь:** `.agent/dashboard/index.html`
**Запуск:** `npx http-server .agent/dashboard -p 8889 -o`
**Данные:** `data.json` (генерируется `projects-health-check.sh`)

---

## Кнопки Footer

### 🔄 Refresh
**Действие:** Перезагружает `data.json` без перезапуска сервера
**Команда:** `fetch('data.json')` (JavaScript)
**Когда использовать:** После ручного запуска health-check

### 🌙 Night Watch
**Действие:** Открывает модалку выбора проектов для ночного рефакторинга
**Скрипт:** `night-watch.sh`
**Команда:** `bash ~/.agent/scripts/night-watch.sh ~/projects/[name] [--dry-run]`
**Что делает:**
- Создаёт ветку `refactor/night-watch-YYYYMMDD`
- Запускает Claude агента с промптом рефакторинга
- Все изменения в отдельной ветке (safe)

### 📊 StatusLine
**Действие:** Устанавливает Smart StatusLine для Claude Code
**Скрипт:** `install-statusline.sh`
**Команда:** `bash ~/.agent/scripts/install-statusline.sh`
**Что делает:**
- Добавляет в `~/.claude/settings.json` кастомный statusline
- Показывает % использования контекста с цветовой индикацией
- Предупреждает при 90%+ (звук + уведомление)

### 🏗️ Setup AI
**Действие:** Открывает модалку установки AI инфраструктуры
**Скрипт:** `setup-ai-infrastructure.sh`
**Команда:** `bash ~/.agent/scripts/setup-ai-infrastructure.sh ~/projects/[name] [--dry-run]`
**Что создаёт:**
```
.agent/
├── MAIN.md           # Single Source of Truth
├── docs/             # Архитектура, конвенции
├── workflows/        # /command workflows
└── scripts/          # Автоматизация

CLAUDE.md             # → Redirect to .agent/MAIN.md
AGENTS.md             # → Redirect to .agent/MAIN.md
.cursorrules          # → Redirect to .agent/MAIN.md
```

### 🧪 Sandbox
**Действие:** Тестирует AI инфраструктуру в изолированной среде
**Скрипт:** `sandbox-test.sh`
**Команда:** `bash ~/.agent/scripts/sandbox-test.sh ~/projects/[name] [--mode lint|smoke]`
**Режимы:**
- `lint` — быстрая проверка структуры (без агента)
- `smoke` — запуск Claude агента в sandbox

### 📝 Log Error
**Действие:** Логирует ошибку в Living Memory журнал
**Скрипт:** `log-error.sh`
**Команда:** `bash ~/.agent/scripts/log-error.sh`
**Что делает:**
- Интерактивно собирает данные об ошибке
- Добавляет запись в `docs/memory/journal/YYYY-MM.md`
- Генерирует ID (ERR-XXX) автоматически
**Параметры (опционально):**
- `--project NAME` — имя проекта
- `--agent NAME` — имя агента
- `--title "TITLE"` — заголовок ошибки

### ⚙️ Settings
**Действие:** Открывает настройки дашборда
**Хранение:** `localStorage`
**Настройки:**
- `optimiPath` — путь к optimi-mac
- `projectsPath` — папка с проектами
- `githubRepo` — репозиторий для ссылок

---

## Actions Dropdown (⚡)

### 🏥 Run Health Check
**Действие:** Запускает полное сканирование проектов
**Скрипт:** `projects-health-check.sh`
**Команда:** `bash ~/.agent/scripts/projects-health-check.sh`
**Результат:** Обновляет `data.json` с информацией о всех проектах

### 📋 Copy Report Path
**Действие:** Копирует путь к последнему отчёту
**Путь:** `~/.agent/reports/health-YYYYMMDD.md`

### 💻 Open Terminal Command
**Действие:** Показывает команду для запуска в терминале
**Команда:** Зависит от контекста

### 🔍 Night Watch (Dry Run)
**Действие:** Предпросмотр Night Watch без изменений
**Команда:** `bash night-watch.sh --dry-run`

---

## Кнопки в таблицах

### 🧠 Triage
**Где:** Колонка Actions в таблицах Working/Attention
**Действие:** Генерирует промпт для анализа проекта
**Скрипт:** `generate-triage-prompt.sh`
**Команда:** `bash ~/.agent/scripts/generate-triage-prompt.sh ~/projects/[name]`
**Результат:** Копирует в буфер промпт для Claude с контекстом проекта

### 📦 Setup (только Attention)
**Где:** Проекты без `.agent/`
**Действие:** Быстрая установка AI инфраструктуры
**Скрипт:** `setup-ai-infrastructure.sh`
**Команда:** `bash ~/.agent/scripts/setup-ai-infrastructure.sh ~/projects/[name]`

---

## Колонки таблиц

### Project
Имя проекта. Клик открывает в Finder.

### .agent/
- ✅ — есть `.agent/` директория
- ❌ — нет инфраструктуры

### HOOK
- 🔴 ACTIVE — есть активный `.agent/HOOK.md`
- ⚪ IDLE — HOOK есть, но не активен
- — — нет HOOK файла

### Git Status
- ✅ clean — нет незакоммиченных изменений
- ⚠️ uncommitted — есть изменения

### Sandbox
- ✅ Lint — прошёл lint тест
- ✅ Smoke — прошёл smoke тест
- ❌ Failed — тест провален
- — — не тестировался

---

## Хранение данных

| Что | Где | Формат |
|-----|-----|--------|
| Проекты | `data.json` | JSON (генерируется) |
| Настройки | `localStorage` | JSON |
| Sandbox результаты | `localStorage.sandboxResults` | JSON |
| Конфиг путей | `config.json` | JSON |

---

## Горячие клавиши

Пока не реализованы. Планируется:
- `R` — Refresh
- `S` — Settings
- `H` — Health Check
