# PixiJS v8 Demo - Agent Simulation

Демонстрация PixiJS v8 с 25 анимированными агентами.

## Возможности

- ✅ 25 анимированных спрайтов (круги разных цветов)
- ✅ Плавное движение с отскоком от стен
- ✅ Пульсирующая scale анимация для каждого агента
- ✅ FPS счётчик в углу (с цветовой индикацией)
- ✅ Отображение количества спрайтов
- ✅ Программно созданные графики (без внешних ассетов)

## Технические детали

**PixiJS v8 Features:**
- `Application` class с тёмным фоном (#1a1a2e)
- `Graphics` API для создания круглых спрайтов
- `Ticker` для animation loop
- `Container` для группировки спрайтов

**Анимация:**
- Каждый агент имеет случайную скорость (1-3 px/frame)
- Отскок от стен с инверсией velocity
- Sin-wave пульсация scale (±15%)
- Delta-time нормализация для плавности

**Производительность:**
- 25 спрайтов @ 60 FPS
- WebGL рендеринг
- Цветовая индикация FPS:
  - 🟢 Зелёный: ≥55 FPS
  - 🟡 Жёлтый: 30-54 FPS
  - 🔴 Красный: <30 FPS

## Установка и запуск

```bash
# Установка зависимостей
pnpm install

# Запуск dev сервера
pnpm start
```

Откройте браузер на `http://localhost:5173`

## Структура файлов

```
pixi-demo/
├── package.json       # PixiJS v8 + Vite
├── index.html         # Canvas container + stats UI
├── main.js            # PixiJS setup + Agent class
└── README.md          # Эта документация
```

## Класс Agent

Каждый агент имеет:
- `x, y` — позиция
- `vx, vy` — velocity
- `color` — уникальный цвет из палитры (25 цветов)
- `pulsePhase` — фаза пульсации
- `sprite` — PixiJS Graphics объект

## Оценка для Agent Colony MVP

**Плюсы:**
- ⚡ Отличная производительность (60 FPS с 25+ спрайтами)
- 📦 Лёгкий bundle (~400KB с зависимостями)
- 🎨 Мощный Graphics API для кастомной визуализации
- 🔧 Простая интеграция с React/Electron

**Минусы:**
- Требует понимания WebGL концептов
- Нет встроенных UI компонентов (нужен отдельный React layer)

**Рекомендация:** PixiJS v8 идеально подходит для RTS-like визуализации агентов в Agent Colony.
