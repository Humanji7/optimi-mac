# Phaser 3 Demo - 25 Animated Sprites

Демонстрация возможностей Phaser 3 для визуализации множественных анимированных объектов (симуляция агентов).

## Особенности

- **25 анимированных спрайтов** с рандомными траекториями
- **Arcade Physics** - отскок от границ canvas
- **7 цветовых вариаций** для визуального разнообразия
- **FPS счётчик** в реальном времени (верхний левый угол)
- **Счётчик спрайтов** (верхний правый угол)
- **Без внешних ассетов** - спрайты генерируются через Graphics API

## Установка

```bash
npm install
```

## Запуск

```bash
npm run dev
```

Откройте браузер по адресу: `http://localhost:5173`

## Технические детали

### Архитектура

- **Phaser 3.86.0** - полноценный game engine
- **Vite** - dev server для быстрой разработки
- **MainScene** - единственная сцена с методами:
  - `preload()` - загрузка ассетов (не используется)
  - `create()` - инициализация спрайтов и UI
  - `update()` - обновление FPS каждый кадр

### Физика

- **Arcade Physics** - лёгкая физика для 2D игр
- `setBounce(1, 1)` - полный отскок (энергия не теряется)
- `setCollideWorldBounds(true)` - запрет выхода за границы
- Случайная скорость от -200 до 200 px/s по X и Y

### Производительность

- **60 FPS** на современном железе (2020+)
- **~20-30 FPS** на слабых устройствах (ожидается)
- **Bundle size:** ~600 KB (Phaser минифицированный)

## Сравнение с другими engines

| Критерий | Phaser 3 | PixiJS | Canvas API |
|----------|----------|--------|------------|
| FPS (25 sprites) | 60 | 60 | 60 |
| Bundle size | ~600 KB | ~200 KB | 0 KB |
| API простота | 🟢 Простой | 🟡 Средний | 🔴 Низкоуровневый |
| Физика | ✅ Встроенная | ❌ Нет | ❌ Нет |
| React интеграция | 🟡 Средняя | 🟢 Хорошая | 🟢 Отличная |

## Плюсы для Agent Colony

✅ **All-in-one:** физика + рендеринг + анимации
✅ **Developer Experience:** минимум boilerplate кода
✅ **Community:** большое сообщество, много примеров

## Минусы для Agent Colony

❌ **Bundle size:** 600 KB критично для Electron (оверкилл)
❌ **Оверхед:** много функций, которые не нужны (audio, input handling)
❌ **React интеграция:** требует обёрток и костылей

## Рекомендация

**Для MVP Agent Colony:** Рассмотреть **PixiJS** или **Canvas API**
**Причина:** Меньший bundle size + лучшая React интеграция
**Phaser уместен если:** планируются сложные игровые механики (коллизии, тайлы, particles)
