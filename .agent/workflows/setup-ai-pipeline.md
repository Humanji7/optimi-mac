# Setup AI Pipeline

---
description: Анализирует проект и генерирует полную AI-инфраструктуру для системной разработки
---

## Как использовать

Вызови `/setup-ai-pipeline` в корне любого проекта. Агент создаст оптимальную конфигурацию для AI-driven разработки.

---

## Инструкции для агента

Ты — архитектор AI-инфраструктуры. Твоя задача — создать полный production-ready конвейер для AI-assisted разработки в этом проекте.

### Фаза 0: Проверка существующей инфраструктуры (ОБЯЗАТЕЛЬНО!)

```bash
# Проверь наличие существующей конфигурации:
ls -la .agent/ CLAUDE.md .cursorrules 2>/dev/null
```

**Если найден ЛЮБОЙ из этих файлов:**
→ **СТОП!** Этот workflow для НОВЫХ проектов.
→ Используй `/upgrade-ai-infrastructure` для обновления существующего.
→ Сообщи пользователю: "Обнаружена существующая AI-инфраструктура. Переключаюсь на умный мерж."

**Если ничего не найдено** → продолжай с Фазы 1.

---

### Фаза 1: Разведка (5 мин)

1. Прочитай `package.json`, `Gemfile`, `pyproject.toml`, `Cargo.toml` — определи стек
2. Изучи структуру директорий — найди паттерны (src/, lib/, tests/, docs/)
3. Проверь существующие конфиги: `.eslintrc`, `tsconfig.json`, `Makefile`
4. Найди CI/CD: `.github/workflows/`, `vercel.json`, `Dockerfile`
5. Оцени размер кодовой базы: `find . -name "*.{js,ts,py,rb}" | wc -l`

**Результат:** Создай `.agent/PROJECT_PROFILE.md` с анализом

### Фаза 2: Генерация .agent/ структуры

Создай следующие файлы:

```
.agent/
├── HOOK.md               # 🪝 GUPP: текущая работа агента (если есть)
├── hooks/                # Архив завершённых Hooks
├── rules.md              # Правила для агента (стиль, ограничения)
├── context.md            # Контекст проекта для быстрого онбординга
├── workflows/
│   ├── feature.md        # Workflow: новая фича
│   ├── bugfix.md         # Workflow: исправление бага
│   ├── refactor.md       # Workflow: рефакторинг
│   ├── test.md           # Workflow: написание тестов
│   └── release.md        # Workflow: подготовка релиза
├── prompts/
│   ├── code-review.md    # Промпт для код-ревью
│   ├── documentation.md  # Промпт для документации
│   └── debugging.md      # Промпт для отладки
└── memory/
    ├── TASKS.md          # Текущие задачи
    ├── DECISIONS.md      # Архитектурные решения
    └── LEARNINGS.md      # Извлеченные уроки
```

### Фаза 3: Git Hooks

// turbo
Создай `.husky/` или `.git/hooks/` с:

```bash
# pre-commit: lint + format
# pre-push: test
# post-commit: update .agent/memory/SESSION_LOG.md
```

### Фаза 4: Генерация rules.md

Файл `.agent/rules.md` должен содержать:

```markdown
# Project Rules for AI Agents

## Stack
- [автозаполнение на основе package.json/etc]

## Code Style
- [взять из eslint/prettier/rubocop конфигов]

## Architecture Constraints
- [на основе структуры проекта]

## Testing Requirements
- [на основе существующих тестов]

## Forbidden Actions
- Не удалять файлы без подтверждения
- Не менять зависимости без объяснения
- Не коммитить напрямую в main/master

## Preferred Patterns
- [извлечь из существующего кода]
```

### Фаза 5: Context Optimization

Создай `.agent/context.md`:

```markdown
# Quick Context for AI Agents

## One-liner
[Что делает проект в одном предложении]

## Key Files
- Entry point: [main.js / app.py / etc]
- Config: [список конфигов]
- Core logic: [ключевые модули]

## Current Focus
[Ссылка на .agent/memory/TASKS.md]

## Don't Touch
[Критичные файлы, legacy, vendor]
```

### Фаза 6: Workflows

Каждый workflow в `.agent/workflows/` должен иметь:

```yaml
---
description: [краткое описание]
---

## Pre-conditions
[что должно быть готово]

## Steps
1. [конкретные шаги]
2. [с командами]
// turbo
3. [где безопасно]

## Verification
[как проверить успех]

## Rollback
[как откатить если что-то пошло не так]
```

### Фаза 7: Финализация

// turbo
1. Запусти `git add .agent/`
2. Создай коммит: `feat: add AI development pipeline infrastructure`
3. Выведи summary что было создано

---

## Критерии успеха

- [ ] Любой AI-агент может прочитать `.agent/context.md` и сразу понять проект
- [ ] Workflows покрывают 80% типовых задач
- [ ] Rules предотвращают типичные ошибки агентов
- [ ] Memory bank готов к использованию
- [ ] Git hooks автоматизируют рутину

---

## Адаптация под стек

### Node.js/TypeScript
- Интеграция с npm scripts
- ESLint/Prettier rules extraction
- Jest/Vitest test patterns

### Python
- pyproject.toml / requirements.txt parsing
- Black/Ruff style extraction
- pytest patterns

### Ruby/Rails
- Gemfile parsing
- RuboCop rules
- RSpec patterns

### Rust
- Cargo.toml parsing
- clippy rules
- cargo test patterns

---

**После выполнения:** Проект готов к AI-driven разработке. Любой агент (Claude, Cursor, Gemini) получит оптимальный контекст с первого запроса.
