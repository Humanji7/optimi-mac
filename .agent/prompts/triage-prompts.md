# Dashboard Triage Prompts

Copy-paste prompts для работы с проблемами из dashboard.

---

## 🚨 No .agent/ Infrastructure

```
Проект: <PROJECT_NAME>
Проблема: Нет .agent/ инфраструктуры

Задача:
1. Инициализируй .agent/ структуру через /setup-ai-pipeline
2. Убедись что создан CLAUDE.md с ссылкой на ~/.agent/GUPP.md
3. Заверши коммитом: "chore: init .agent/ infrastructure"
```

**Пример:**
```
Проект: sphere-777
Проблема: Нет .agent/ инфраструктуры

Задача:
1. Инициализируй .agent/ структуру через /setup-ai-pipeline
2. Убедись что создан CLAUDE.md с ссылкой на ~/.agent/GUPP.md
3. Заверши коммитом: "chore: init .agent/ infrastructure"
```

---

## ⚠️ Uncommitted Changes

```
Проект: <PROJECT_NAME>
Проблема: Uncommitted changes

Задача:
1. Покажи git status и git diff --stat
2. Объясни что изменено (1-2 строки)
3. Рекомендация: коммитить / ревью / откат
4. Если safe → сделай коммит с осмысленным message
```

**Пример:**
```
Проект: bip-buddy
Проблема: Uncommitted changes

Задача:
1. Покажи git status и git diff --stat
2. Объясни что изменено (1-2 строки)
3. Рекомендация: коммитить / ревью / откат
4. Если safe → сделай коммит с осмысленным message
```

---

## 🪝 Active HOOK

```
Проект: <PROJECT_NAME>
Проблема: Активный HOOK — незавершённая работа

Задача:
1. Прочитай .agent/HOOK.md
2. Покажи статус convoy (molecules done / current / remaining)
3. Спроси: продолжить работу или архивировать HOOK?

Если продолжить → команда: "Продолжи"
Если архивировать → "Архивируй HOOK"
```

**Пример:**
```
Проект: personal-site
Проблема: Активный HOOK — незавершённая работа

Задача:
1. Прочитай .agent/HOOK.md
2. Покажи статус convoy (molecules done / current / remaining)
3. Спроси: продолжить работу или архивировать HOOK?
```

---

## 🔴 No Git Repository

```
Проект: <PROJECT_NAME>
Проблема: Нет git репозитория

Задача:
1. Проверь что проект не временный (покажи ls -la)
2. Инициализируй git: git init
3. Создай .gitignore если нужен
4. Сделай initial commit
```

**Пример:**
```
Проект: tg-tr-7
Проблема: Нет git репозитория

Задача:
1. Проверь что проект не временный (покажи ls -la)
2. Инициализируй git: git init
3. Создай .gitignore если нужен
4. Сделай initial commit
```

---

## 🔄 Batch Auto-Fix (Multiple Projects)

```
Dashboard показывает <N> проектов Need Attention.

Задача: Auto-fix safe issues в batch режиме

Проекты с "No git":
- <list>

Проекты с "No .agent/":
- <list>

Проекты с uncommitted (требуют ревью):
- <list>

План:
1. Auto-fix "No git" → git init + initial commit
2. Auto-fix "No .agent/" → /setup-ai-pipeline
3. Показать uncommitted для ревью

Начинай с Step 1.
```

---

## Quick Reference

| Issue | Safe Auto-Fix? | Command |
|-------|----------------|---------|
| No .agent/ | ✅ Yes | `/setup-ai-pipeline` |
| No git | ✅ Yes | `git init` |
| Uncommitted | ❌ Review first | `git status && git diff` |
| Active HOOK | ❌ Manual | Read HOOK.md |

---

*v1.0 — Triage Prompts*
