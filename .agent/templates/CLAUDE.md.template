# CLAUDE.md — Global AI Agent Rules

> Этот файл читается автоматически при старте ЛЮБОЙ сессии.
> Применяется ко ВСЕМ проектам.

---

## 🪝 GUPP: Universal Propulsion Principle

> **"Если на твоём Hook есть работа — ТЫ ОБЯЗАН её выполнить"**

### ПЕРВОЕ ДЕЙСТВИЕ В ЛЮБОЙ СЕССИИ:

```bash
cat .agent/HOOK.md 2>/dev/null | head -30
```

| GUPP Status | Действие |
|-------------|----------|
| **ACTIVE 🔴** | Выполни CURRENT molecule. Не принимай новую работу. |
| **IDLE ⚪** | Свободен. Можно принимать новую задачу. |
| **Файл не найден** | Работай обычно, но помни о правилах ниже. |

---

## 📏 Threshold Rules

### При задаче >3 файлов:
```
→ СТОП! Сначала /decompose
→ Создай .agent/HOOK.md с molecules
→ Работай по одному molecule за раз
```

### При 10+ tool calls в сессии:
```
→ Рассмотри /handoff
→ Коммитни прогресс
→ Обнови HOOK.md
```

### При 5+ изменённых файлов:
```
→ Обязательный промежуточный коммит
→ Обнови HOOK.md если есть
```

---

## 🔄 /handoff Protocol

Когда приближаешься к лимиту контекста:

1. **Коммит:**
   ```bash
   git add . && git commit -m "WIP: [molecule] - handoff"
   ```

2. **Обнови HOOK.md:**
   - Пометь текущий molecule
   - Добавь Handoff Note
   - Укажи точку продолжения

3. **Сообщи пользователю:**
   ```
   ✅ Completed: M1, M2
   🔴 Current: M3 (step 2/4)
   ⚪ Pending: M4, M5
   
   Resume: "Продолжи" в новом чате
   ```

---

## 🎯 Molecule Execution

Для каждого molecule:
1. Объяви: "Начинаю M[N]: [название]"
2. Выполни ТОЛЬКО этот molecule
3. Обнови HOOK.md: M[N] → DONE ✅
4. Коммит: `git commit -m "M[N]: [описание]"`
5. Самопроверка: продолжить или handoff?

---

## 🚫 Запрещено

- ❌ Игнорировать существующий HOOK.md
- ❌ Делать >5 файлов без коммита
- ❌ Пропускать handoff при признаках лимита
- ❌ Удалять HOOK.md без архивации

---

## 📂 Workflows (в каждом проекте)

| Команда | Описание |
|---------|----------|
| `/decompose` | Разбить задачу на molecules |
| `/anti-crash-rules` | GUPP справочник |
| `/setup-ai-pipeline` | Создать .agent/ с нуля |
| `/upgrade-ai-infrastructure` | Умный мерж существующего |

---

## 🗣️ User Commands

| Пользователь говорит | Агент делает |
|---------------------|--------------|
| "Продолжи" | Читает HOOK.md, выполняет CURRENT |
| "Статус" | Показывает прогресс convoy |
| "Следующий" | Только один molecule |
| "/handoff" | Сохраняет и готовит передачу |
| "Декомпозиция: X" | Создаёт HOOK.md для задачи X |

---

*Last updated: 2026-01-10*
*Source: Gas Town patterns (MEOW, GUPP, Hooks)*
